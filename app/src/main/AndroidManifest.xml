<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LimbusApp"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.LimbusApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CreateAccountActivity"
            android:label="Crear cuenta"
            android:exported="false" />

        <activity
            android:name=".EmailPasswordLoginActivity"
            android:label="Ingresar datos"
            android:exported="false" />

        <activity
            android:name=".LoginActivity"
            android:label="Iniciar Sesión"
            android:exported="false" />

        <!-- Keeping InstitutionsListActivity but removing the institution registration -->
        <activity
            android:name=".InstitutionsListActivity"
            android:label="Instituciones Registradas"
            android:exported="false" />

    </application>

</manifest>